<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Tech Blog</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <link rel="stylesheet" href="/css/style.css" />
    <link href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css" rel="stylesheet">
</head>

<body>
    <div class="navbar has-background-black is-justify-content-space-around">
        <header>
            <h1 class="tech-title has-text-white is-size-1">The Tech Blog</h1>
        </header>
        <nav class="is-flex is-align-items-center">
            {{#if loggedIn}}
            <a href="/" class="is-size-3">Home</a>
            <a href="/dashboard" class="is-size-3">Dashboard</a>
            <button id="logout">Logout</button>
            {{else}}
            <a href="/login" class="is-size-2 p-3">Login</a>
            <a href="/signup" class="is-size-2 p-3">Signup</a>
            {{/if}}
        </nav>
    </div>
    <main>
        {{{body}}}
    </main>


    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>